# =================================
# Court of Shadows - Configuration
# =================================

# Server Configuration
NODE_ENV=development
PORT=3000

# Database Configuration (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=courtdb
DB_USER=appuser
DB_PASSWORD=your_secure_password_here

# =================================
# Production Settings (optional)
# =================================
# NODE_ENV=production
# For Google Cloud SQL, the host is configured automatically
